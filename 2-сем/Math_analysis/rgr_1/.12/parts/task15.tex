\documentclass[../rgr1.tex]{subfiles}

\begin{document}

\Problem{
	Довести за допомогою формули Лагранжа
	$nb^{n-1}(a - b) < a^n - b^n < na^{n-1} (a - b)$,
	якщо $n > 1,~a > b$
}

\Solution

\begin{equation}
	nb^{n-1}(a - b) < a^n - b^n < na^{n-1} (a - b)
	<++>
\end{equation}

\Answer{<++>}
\end{document}
